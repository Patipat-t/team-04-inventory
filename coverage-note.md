# Coverage Note - Lab 5

## 1. บรรทัดที่ test ยังไม่เคยรันผ่านและความเสี่ยง
จากผลการวัด Coverage บรรทัดที่ยังไม่ถูกทดสอบ ได้แก่:
- `src/ebook_service.py` บรรทัดที่ 10 และ 22
- โมดูล legacy และบริการหลัก เช่น `src/pricing_legacy.py`, `src/service.py`, `src/main.py`

**ความเสี่ยง:** บรรทัดที่ไม่ได้ถูกรันผ่าน Test อาจมี Logic Error, Unhandled Exception หรือการจัดการกรณีผิดปกติที่คลาดเคลื่อน ซึ่งอาจส่งผลให้ระบบล่ม (Crash) หรือทำงานผิดพลาดในสภาวะจริงโดยไม่มีระบบแจ้งเตือนล่วงหน้า

## 2. ทำไม Coverage 100% ถึงยังไม่ได้แปลว่า Test ดี
Coverage 100% บอกเพียงแค่ว่า "โค้ดทุกบรรทัดถูกรันผ่านอย่างน้อย 1 ครั้ง" แต่ไม่ได้การันตีว่า:
- มีการใช้ `assert` ตรวจสอบความถูกต้องของผลลัพธ์อย่างรัดกุมหรือไม่
- ครอบคลุม Edge Cases, Boundary Values หรือ Invalid Inputs ทั้งหมดหรือไม่
**ตัวอย่าง:** ใน `src/inventory.py` แม้จะมี Coverage 100% แต่ถ้าเราเขียนเพียงแค่รันฟังก์ชันโดยไม่มีการ `assert` ค่า หรือไม่ได้ทดสอบกรณีการใส่ข้อมูลผิดประเภท (เช่น ใส่ข้อความแทนตัวเลข) ระบบก็อาจจะผ่าน Test ไปได้ทั้งที่ยังมีบั๊กซ่อนอยู่

## 3. ถ้ามีเวลาเขียน Test เพิ่มได้แค่ 3 ข้อ จะเลือกเขียนข้อไหนก่อน
1. **Test การคิดคำนวณราคาและส่วนลด (`src/pricing_legacy.py`):** เนื่องจากเป็นส่วนที่เกี่ยวข้องกับระบบการเงินโดยตรง ความผิดพลาดในจุดนี้ส่งผลกระทบต่อธุรกิจสูงที่สุด
2. **Test การตรวจสอบสิทธิ์ดาวน์โหลด E-Book (`src/ebook_service.py` บรรทัด 10, 22):** เพื่ออุดช่องโหว่ความปลอดภัย ป้องกันไม่ให้ผู้ใช้ที่ยังไม่ยืนยันคำสั่งซื้อเข้าถึงลิ้งก์ดาวน์โหลดได้
3. **Test การขายสินค้าเกินจำนวนคงคลัง (`src/inventory.py` - `sell` Edge Case):** เพื่อป้องกันปัญหาสินค้าคงคลังติดลบและการทำธุรกรรมที่ไม่สมบูรณ์
