# Code Smells Analysis (`pricing_legacy.py`)

จากการวิเคราะห์โค้ดเดิมในโมดูลคิดราคาและส่วนลด พบจุดที่เป็นปัญหา (Code Smells) ดังนี้:

1. **Magic Numbers:** มีการใช้ตัวเลขดิบ เช่น `0.1`, `0.15`, `0.05`, `50`, `100` กระจายอยู่ในเงื่อนไขคำนวณ โดยไม่มีการตั้งชื่อตัวแปรคงที่ (Constants) ทำให้ไม่ทราบความหมายที่แท้จริง
2. **Complex Conditional Logic:** มีการใช้ `if-elif-else` ซ้อนกันหลายชั้น ทำให้ไล่สายตาอ่านลำดับการทำงานได้ยาก และเสี่ยงต่อการเกิดบั๊กเมื่อแก้ไข
3. **Poor Variable Naming:** ตัวแปรและพารามิเตอร์ใช้ชื่อย่อสั้นๆ เช่น `p`, `q`, `t`, `d` ทำให้ต้องเสียเวลาแปลความหมายว่าแต่ละตัวแปรแทนข้อมูลอะไร
4. **Duplicated Code:** มีการเขียนสูตรคำนวณส่วนลดและการปัดเศษซ้ำไปซ้ำมาในหลายบล็อกเงื่อนไข แทนที่จะรวบยอดคำนวณในจุดเดียว
5. **Lack of Type Hints and Docstrings:** ฟังก์ชันไม่มีการระบุ Type Annotation และไม่มี Docstring อธิบายพฤติกรรม ทำให้เดาชนิดข้อมูลและพฤติกรรมได้ยาก
